{
  "name": "@local-mono",
  "type": "module",
  "devDependencies": {
    "@wdio/cli": "^8.6.9",
    "@wdio/cucumber-framework": "^8.6.9",
    "@wdio/junit-reporter": "^8.6.8",
    "@wdio/local-runner": "^8.6.9",
    "@wdio/spec-reporter": "^8.6.8",
    "chromedriver": "^111.0.0",
    "ts-node": "^10.9.1",
    "tsup": "^6.7.0",
    "typescript": "^5.0.3",
    "wdio-chromedriver-service": "^8.1.1"
  },
  "scripts": {
    "config": "npm run build -w=@local-mono/config",
    "wdio": "npm run wdio -w=@local-mono/config",
    "build": "npm run build -w=@local-mono/pages",
    "steps": "npm run build -w=@local-mono/content"
  },
  "workspaces": [
    "libs/*",
    "test/content"
  ]
}
